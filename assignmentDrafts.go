package main

/*

//IGNORE THIS FILE, IT IS USED TO CREATE PART A

var name string //Starts with a letter
var _name string //Starts with underscore
var 1name string //Can't start with a digit
var aBcDeFgH0123_321 int //Only alpha-numeric characters and underscores
var cant-Use!Special*Characters int //Can't use special characters or hyphen (-)
var age string //These three 	}
var Age string //are different  } Case Sensitive
var AGE string //variables		}
var exampleOfAVeryVeryVeryLongVariableNameWhichIsStillValid string //No Length Limit
var user name string //Can't use spaces
var case string // "case" is a keyword in Go

var PascalCase string //Each word starts with a capital letter
var camelCase string //Each word starts with a capital letter, except the first word
var snake_case string //Each word is connected with an underscore (_) character

// Basic assignment ':='
	x := 10 //Declares and assigns variable x

	// Basic assignment '='
	var y = 2 //Declares and assign variable x

	// Compound assignment operators '-=' or '+=' or any other operators
	x -= 5 //Change the value of x to x-5

	// Unary assignment '++' or '--'
	x++ //increment x

	// Multiple values assignment
	x, z := 20, 10 //Declares and assigns variable x=20 and z=10

	// Swapping values assignment
	x, z = z, x //Swap values of x and z

	x++ //Increment x (x+1)
	--x //Decrement x (x-1)


	fmt.Println(x, y, z)

// Return multiple values function
// It is usually used for errors
// The function below checks if the number given is dividable
func divide(a, b float64) (float64, error) {
	//Receives two numbers 'a' and 'b'
	//Returns a data type float and error message
	if b == 0 { //If b is 0
		//Any numbers can't be divided by zero
		return 0, errors.New("division by zero") //Returns value 0 with error message
	}
	//If b is not 0
	return a / b, nil //Returns the value of division with no errors
}

func methodMain() { //Main function
	//Function to return variables
	a := getNumbers() //Calls the function and store the returned value to 'a'
	fmt.Println(a)
}

type Pigeon struct {
	Name          string //public keyword - "Name" starting with an uppercase
	featherLength int    //private keyword - "featherLength" starting with lowercase
}
*/
